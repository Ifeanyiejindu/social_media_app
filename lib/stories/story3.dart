import 'package:flutter/material.dart';

class StoryThree extends StatelessWidget {
  const StoryThree({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.grey,
    );
  }
}
