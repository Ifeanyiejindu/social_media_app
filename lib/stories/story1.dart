import 'package:flutter/material.dart';

class StoryOne extends StatelessWidget {
  const StoryOne({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.red,
    );
  }
}
