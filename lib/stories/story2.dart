import 'package:flutter/material.dart';

class StoryTwo extends StatelessWidget {
  const StoryTwo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.orange,
    );
  }
}
